name: build inputs mapping action
concurrency:
  group: build-inputs-mapping-action
on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/build-inputs-mapping-action.yaml
      - inputs-mapping/src/**/*
      - inputs-mapping/package*.json
      - inputs-mapping/*.js

permissions:
  contents: write

env:
  AUTHOR_EMAIL: admin+github-actions@tapclap.com
  AUTHOR_NAME: 'Github Actions'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: actions/setup-node@main
        with:
          node-version: 20

      - name: node modules
        working-directory: inputs-mapping
        run: npm ci

      - name: build
        working-directory: inputs-mapping
        run: npm run build


      - name: commit builded action
        run: |
          if git status | grep 'inputs-mapping/dist/' -q;
          then
            git config --global user.email "${AUTHOR_EMAIL}"
            git config --global user.name "${AUTHOR_NAME}"
            git add ./inputs-mapping/dist
            git commit -m "builded inputs-mapping action"
            while true; do
              if ! git push origin ${{ github.ref }}:${{ github.ref }}; then
                echo "Push failed, sleep 10 seconds and retrying..."
                git pull --rebase
                sleep 10
                continue
              else
                break
              fi
            done
          fi